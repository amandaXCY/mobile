const { globalObjectKey } = require('../constants');

var { buildProd } = global[globalObjectKey];

module.exports = {
  loader: 'style-loader',
  options: {
    injectType: buildProd ? 'singletonStyleTag' : 'styleTag'
  }
};
