export  default  function (id) {
     var node = document.createElement('div');
        node.attributes.id=name;
        node.attributes.clas ="box"
        document.body.appendChild(node)

    return document.getElementById(id)
}